# ── BioLab AI backend dependencies ───────────────────────────────────────────
# Install with:  pip install -r requirements.txt

# Web framework + server
fastapi
uvicorn[standard]
python-multipart          # required by FastAPI for Form()/UploadFile
pydantic

# Data handling
pandas
openpyxl                  # .xlsx reading for pandas.read_excel
xlrd                      # .xls reading for pandas.read_excel

# Azure OpenAI. Azure's v1 API surface is OpenAI-compatible, so the stock client
# talks to it directly given a base_url — no azure-specific package needed.
openai

# Database drivers
mysql-connector-python
psycopg2-binary

# MCP server (SSE tools)
fastmcp

# Optional: load environment variables from a .env file (see .env.example)
python-dotenv
